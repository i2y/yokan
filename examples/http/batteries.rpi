# Generated by rpi-gen — do not edit. Regenerate from rustdoc JSON.

class Kernel {
  static fn envVar(name: String) String? @rust("pixie_kernel::env_var")
  static fn listDir(path: String) !List<String> @rust("pixie_kernel::list_dir")
  static fn sleepMs(ms: Int) @rust("pixie_kernel::sleep_ms")
}
# skipped (unbindable in v0):
#   completion — unbounded generic `T`
#   map_from_send — param `pairs`: unsupported type `&[<complex>]`
#   map_to_send — param `m`: unsupported type `&Map`
#   mount — generic `C` has non-string bounds
#   set_worker_spawner — generic `impl Fn(Box<dyn FnOnce() + Send>) + Send + Sync + 'static` has non-string bounds
#   spawn_worker — generic `impl FnOnce() + Send + 'static` has non-string bounds

class Http {
  static fn get(url: String) !String @rust("pixie_kernel::http::get")
  static fn getBytes(url: String) !Bytes @rust("pixie_kernel::http::get_bytes")
  static fn getWith(url: String, headers: Map<String, String>) !String @rust("pixie_kernel::http::get_with")
  static fn post(url: String, body: String) !String @rust("pixie_kernel::http::post")
  static fn postWith(url: String, body: String, headers: Map<String, String>) !String @rust("pixie_kernel::http::post_with")
}
