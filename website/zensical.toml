[project]
site_name = "Yokan"
site_url = "https://i2y.github.io/yokan/"
site_description = "A native compiler for a statically typed subset of Python, for desktop apps."
site_author = "Yasushi Itoh"
copyright = "Copyright &copy; 2026 Yasushi Itoh &mdash; MIT OR Apache-2.0"

docs_dir = "docs"
site_dir = "build"
use_directory_urls = true

# Site styling: the hero, and the frame around the app screenshots.
extra_css = ["stylesheets/extra.css"]

nav = [
  { Home = "index.md" },
  { Installation = "installation.md" },
  { "Language tour" = [
    { "First app" = "tour.md" },
    { "Flow and data" = "tour-logic.md" },
    { "Values and types" = "tour-values.md" },
    { "Components and style" = "tour-ui.md" },
    { "Libraries and crates" = "tour-lib.md" },
    { "Verify and ship" = "tour-ship.md" },
  ] },
  { Demos = "demos.md" },
]

# ---- Language switcher ----------------------------------------------------

[[project.extra.alternate]]
name = "English"
link = "/yokan/"
lang = "en"

[[project.extra.alternate]]
name = "日本語"
link = "/yokan/ja/"
lang = "ja"

[project.theme]
variant = "modern"
# navigation.footer  previous / next at the foot of every page, so the
#                    tour reads in order without the sidebar
# navigation.top     a back-to-top button on long pages
# navigation.tracking  the address bar follows the section being read
# content.code.copy  a copy button on every code block
features = [
  "navigation.footer",
  "navigation.top",
  "navigation.tracking",
  "content.code.copy",
]
logo = "images/logo.svg"
favicon = "images/favicon.png"

# ---- Palette --------------------------------------------------------------

# Yokan the sweet: dark azuki block, a slice of chestnut. Black
# header keeps the dark app screenshots seamless; amber is the
# accent. Same pair on both schemes so the brand holds.

[[project.theme.palette]]
media = "(prefers-color-scheme: light)"
scheme = "default"
primary = "black"
accent = "amber"
toggle.icon = "lucide/moon"
toggle.name = "Switch to dark mode"

[[project.theme.palette]]
media = "(prefers-color-scheme: dark)"
scheme = "slate"
primary = "black"
accent = "amber"
toggle.icon = "lucide/sun"
toggle.name = "Switch to light mode"

# ---- Markdown extensions ---------------------------------------------------

# Unicode-preserving heading anchors (Japanese headings slug like
# GitHub's), via pymdownx's slugifier.
[project.markdown_extensions.toc]
slugify = { object = "pymdownx.slugs.slugify", kwds = { case = "lower" } }

[project.markdown_extensions.attr_list]
[project.markdown_extensions.admonition]
[project.markdown_extensions.md_in_html]

[project.markdown_extensions.pymdownx.highlight]
anchor_linenums = true
line_spans = "__span"
pygments_lang_class = true

[project.markdown_extensions.pymdownx.superfences]
[project.markdown_extensions.pymdownx.emoji]
emoji_generator = "zensical.extensions.emoji.to_svg"
emoji_index = "zensical.extensions.emoji.twemoji"
