test/refuse/range_map.go:9:20: Gomamochi cannot take this — `range` over a map walks it in a different order every run, and the two runs would draw different screens. Collect the keys, sort them, and range over those
    	for name := range a.prices {
    	                  ^
