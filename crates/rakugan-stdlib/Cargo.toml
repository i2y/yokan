[package]
name = "rakugan-stdlib"
# Written out rather than inherited: the binding cache keys an entry on
# the crate's own version, and it reads this file, not the workspace's.
version = "0.1.0"
edition.workspace = true
license.workspace = true
