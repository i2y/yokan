# A project-style Yokan app: the crate declaration lives here, in
# [tool.yokan.crates], instead of a PEP 723 block.
[project]
name = "proj-demo"
version = "0.1.0"
requires-python = ">=3.14"

[tool.yokan.crates]
hexfmt = { path = "../native/hexfmt" }
